# Lab 3

## 实现的功能

1. 移植上一个实验的功能
2. 实现了 `spawn`，只需要创建任务并设置父子关系
3. 添加了 `TaskStrider` 结构，作为 `TaskControlBlockInner` 的一个字段，存储调度相关的信息
4. 把 `TaskManager` 内部存储任务的 `VecDeque` 更换为 `BinaryHeap`，实现调度

## 问答题

1. 一个时间片后，理论上 `p2.stride` 应该是 260，但是由于溢出，实际会变成 4，比 `p1.stride` 小，下一次还是 p2 执行
2. 设 pass 的最大值为 `PASS_MAX`，则根据条件有 `PASS_MAX <= BigStride / 2`，在不考虑溢出的情况下，又显然有 `STRIDE_MAX – STRIDE_MIN <= PASS_MAX`，得到结论
3. ```rust
   impl PartialOrd for Stride {
       fn partial_cmp(&self, other: &Self) -> Option<Ordering> {
           ((self.0 - other.0) as i64).partial_cmp(&0)
       }
   }
   ```

## 荣誉准则

1. 在完成本次实验的过程（含此前学习的过程）中，我曾分别与 以下各位 就（与本次实验相关的）以下方面做过交流，还在代码中对应的位置以注释形式记录了具体的交流对象及内容：
   无
2. 此外，我也参考了 以下资料 ，还在代码中对应的位置以注释形式记录了具体的参考来源及内容：
   无
3. 我独立完成了本次实验除以上方面之外的所有工作，包括代码与文档。我清楚地知道，从以上方面获得的信息在一定程度上降低了实验难度，可能会影响起评分。
4. 我从未使用过他人的代码，不管是原封不动地复制，还是经过了某些等价转换。我未曾也不会向他人（含此后各届同学）复制或公开我的实验代码，我有义务妥善保管好它们。我提交至本实验的评测系统的代码，均无意于破坏或妨碍任何计算机系统的正常运转。我清楚地知道，以上情况均为本课程纪律所禁止，若违反，对应的实验成绩将按“-100”分计。
